name: Auto Push to Main

on:
  schedule:
    - cron: "0/5 * * * *"

  push:
    branches:
      - develop  
    
jobs:
  merge_to_main:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        env:
          ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
                  
      - name: Merge Changes
        run: |
          git config --global user.name "Satyajeet256"
          git config --global user.email "snpaygude@gmail.com"
          git checkout main
          git pull origin main
          if git merge --no-ff develop; then
            echo "Merge successful."
          else
            echo "No changes to merge."
            exit 78
          fi
          
      - name: Add New Files
        if: steps.merge.outcome == 'success'
        run: |
          git config --global user.name "Satyajeet256"
          git config --global user.email "snpaygude@gmail.com"
          git diff --name-only --diff-filter=A develop | xargs -I % git checkout main -- %

      - name: Commit and Push Changes
        if: steps.merge.outcome == 'success'
        run: |
          git config --global user.name "Satyajeet256"
          git config --global user.email "snpaygude@gmail.com"
          git add .
          git commit -m "Merge changes from develop"
          git push origin main
